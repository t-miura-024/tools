use_default_settings: true

general:
  instance_name: "mt-searxng"

search:
  formats:
    - html
    - json

server:
  # NOTE: This is the SearXNG upstream default secret key, kept here so the
  # local dev container starts with a stable, well-known value. It is NOT a
  # real credential and must be regenerated via `openssl rand -hex 32` before
  # any public-facing deployment.
  secret_key: "475596bd4ce23c88a22751a1310985930e43b87998529618a48c5b125d6bcfb6"
  limiter: false
  port: 8080
  bind_address: "0.0.0.0"
